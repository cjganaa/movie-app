export type Movie = {
  id: number,
  title: string,
  poster: string,
  rating: number,
}

export type MovieInfo = {
  id: number,
  adult: boolean,
  background_img: string,
  language: string,
  title:string,
  overview:string,
  rate:number,
}